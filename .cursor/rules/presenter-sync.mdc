---
description: Regole per sincronizzazione MainWindow-PresenterWindow
globs: "**/PresenterWindow.xaml.cs,**/PresenterSyncService.cs,**/MainWindow.xaml.cs"
alwaysApply: false
---

# Sincronizzazione Presenter

## Architettura sync
```
MainWindow (editor/preview)
    │
    ├── SyncPresenterDocument() ──→ clone immediato XamlPackage
    │       usare per: load file, cambio colore, cambio font
    │
    ├── RequestPresenterSync() ──→ clone debounced 300ms
    │       usare per: digitazione utente (TextChanged)
    │
    ├── SyncPresenterScroll() ──→ SetVerticalOffset(offset)
    │       usare per: scroll position (ScrollChanged); stesso documento = stesso offset
    │
    ├── UpdatePresenterArrowAppearance() ──→ colore, scala (NON posizione)
    │       usare per: modifica colore/dimensione freccia; NON aggiorna posizione (evita spostamenti indesiderati)
    │
    └── SyncPresenterAppearance() ──→ sfondo, freccia (colore, scala, Y assoluta)
            usare per: sync completo prima di Show presenter o dopo cambio layout
    │
    ▼
PresenterWindow (output esterno)
```

## Regole di sync scroll
- USARE `SetVerticalOffset(offset)` — stesso documento clonato = stesso layout = stesso offset mostra testo identico.
- `SetVerticalOffset` usa coalescing: `Dispatcher.BeginInvoke(DispatcherPriority.Render)` per evitare flood durante scroll veloce.
- Durante auto-scroll: `OnScrollRendering` chiama `SetVerticalOffset` direttamente; `ScrollChanged` salta sync se `_isAutoScrolling`.
- Throttling: `UpdateScrollProgressDisplay` e `NotifyOscPosition` max ogni 50ms (ScrollNotifyMinIntervalMs).

## Regole di sync freccia
- **Y assoluta**: `SetArrowAbsoluteY(double top)` — posizione in pixel dal top della viewport. Stessa Y = stessa riga di testo (riferimento testo, non schermo).
- **X normalizzata**: `SetArrowNormalizedX(double)` per posizione orizzontale.
- In mirror mode: la freccia nel presenter si sposta a destra, il testo si specchia.
- **Freccia stabile**: `UpdatePresenterArrowAppearance()` invia `SetArrowAbsoluteY(arrowTop)` dalla MainWindow; non usa coordinate normalizzate Y.

## Regole di sync documento
- PresenterSyncService: flag `_isSyncing` evita re-entrancy; se sync in corso, marca dirty e risincronizza dopo.
- Il clone copia TUTTE le proprieta document-level: PagePadding, LineHeight, TextAlignment, Background, FontFamily, FontSize, Foreground, FontWeight, FontStyle.
- Il padding nel PresenterWindow viene ricalcolato da `GetPresenterPagePadding()` in base al mirror mode.
- In mirror mode: swap left/right nel padding E nella trasformazione del contenuto.

## Regole di sync colori
- Quando cambia il colore sfondo nella MainWindow, chiamare `_presenterWindow.SetBackgroundColor(color)` per aggiornare ANCHE i border container del presenter.
- Il FlowDocument.Background da solo non basta: i border attorno restano scuri se non aggiornati.

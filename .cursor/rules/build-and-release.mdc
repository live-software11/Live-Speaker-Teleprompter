---
description: Build, clean e release Live Speaker Teleprompter
globs: "**/build-installer.ps1,**/clean-and-build*"
alwaysApply: false
---

# Build e Release

## Script principale (root)
- **clean-and-build.ps1** / **clean-and-build.bat** — pulisce bin/obj/portable e crea build completa
- Lanciare dalla root: `.\clean-and-build.ps1` o doppio clic su `clean-and-build.bat`

## Pipeline (clean-and-build.ps1)
1. Pulizia bin/obj/portable
2. Genera icona: `scripts\convert-logo.ps1` (da `icons\Logo Teleprompter.png` → `icons\app-icon.ico`)
3. dotnet restore
4. build-installer.ps1: publish + portable EXE + installer EXE (no ZIP)

## Icona
- `icons\Logo Teleprompter.png` → `scripts\convert-logo.ps1` → `icons\app-icon.ico`
- L'icona è generata automaticamente da clean-and-build prima del publish.

## Output (portable/)
- `Live-Speaker-Teleprompter-Portable.exe` — eseguibile standalone, nessuna dipendenza
- `Live-Speaker-Teleprompter-Installer.exe` — installer con collegamenti Start/Desktop

## Pulizia
- bin/, obj/, publish/ — ignorati da git
- portable/*.exe, *.zip, *.msi — ignorati da git (artefatti build)
